# 模块数据字典模板

复制本模板为 `<module>.md`，只设计自己模块拥有的表。第一版先评审文本，不直接提交个人 `.accdb`。

## 1. 模块与负责人

- 模块：
- 对应 Epic：
- 状态：草稿 / 已评审 / 已实现

## 2. 表清单

| 表名 | 业务含义 | 主键 | 重要约束 |
|---|---|---|---|
| `tblExample` | 示例 | `exampleId` | 业务唯一字段、状态字段 |

## 3. 字段字典

### `tblExample`

| 字段 | Access 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `exampleId` | Long Integer | 是 | 自动编号 | 主键 |
| `status` | Short Text(20) | 是 | `ACTIVE` | 禁止随意物理删除 |

## 4. 关联与索引

- 外键只写逻辑含义和目标主键，不跨模块更新目标表。
- 列出唯一索引、普通索引以及删除/更新策略。

## 5. 演示数据

列出最少一组正常数据、一组边界数据；不得使用真实个人隐私。

## 6. 待评审问题

- 字段、类型或跨模块依赖中尚未决定的事项。
