# 项目当前状态

更新时间：2026-09-11

本文只保留“现在做到哪里、下一步做什么”。具体提交过程和讨论以 Git、Pull Request、Issue 为准。

## 1. 已完成的公共基础

- Maven 三模块骨架和统一启动入口。
- Swing 统一登录、按会话权限显示的校园服务导航、Socket 请求/响应、Action 路由。
- 登录页和登录后工作区已统一为青绿色视觉主题，包含账号状态、修改密码、退出登录和服务器连接检测。
- 已建立全系统《软件设计说明书》汇总框架，公共架构和用户登录部分已完成，其余子系统章节等待负责人材料。
- Access 一卡通账号持久化、带空闲和绝对有效期的内存 token 会话、`Role` 与 `AdminScope` 基础授权；最终种子库含 `20260000`—`20260038` 共 39 个账号，普通账号只允许 0–1 个子系统管理权。
- Access 公共教师档案含 8 名教师；账号管理页提供“教师管理”入口，超级管理员可批量导入、新增、修改及取消教师资格。选课模块通过 `tblOfferingTeacher` 按 `teacherUserId` 维护任课关系，不再使用临时硬编码映射。
- 学籍档案、商店商品/库存/余额/购物车/订单均已接入同一 Access 数据库；最终演示数据库可以停服后自动备份、临时生成、校验并原子替换。
- 六个客户端/服务器模块的注册结构。
- 医院医生新增采用分类申请审核：已有账号由医院明确选择并校验，外来医生由用户模块在批准后按当年最大流水号加一生成一卡通号；医院管理员可在申请记录中取得审核状态和生成的一卡通号，再交付给医生。申请和档案保存在 Access。
- 登录失败保护：同一一卡通号在 10 分钟内连续失败 5 次后暂停登录 5 分钟；成功登录会清除失败记录，失败响应不暴露账号是否存在。
- 单服务器、多客户端网络链路已实现；跨电脑统一使用 Radmin VPN，应用层保持 TCP Socket。自动测试验证服务器监听全部网卡、并发客户端具有独立 token，单个客户端退出不会结束其他客户端的会话。Radmin VPN 真机联调仍需在两台真实设备上验收。
- 用户模块已增加服务器本地超级管理员紧急改密，并验证并发创建账号不会产生重复一卡通号。跨模块资源竞争仍需分别验收。

## 2. 六个模块

| 模块 | 当前可用内容 | 紧接着要完成 |
|---|---|---|
| 用户管理 | 登录、退出、登录失败限制、30 分钟空闲/8 小时绝对会话过期和修改密码；超级管理员自动编号新增、单个维护及 CSV 批量导入账号；普通账号最多一个子系统管理权；账号管理操作审计与查看；为已批准的子系统申请开通账号；Access 账号及审计 DAO；每账号独立盐值的 PBKDF2 慢哈希和旧凭据登录后自动迁移 | 完整账号状态模型及真实网络 TLS 方案 |
| 学生学籍 | 学籍查询、自助资料维护、学籍异动申请与管理员审核、证明生成和基于角色的访问校验 | Access DAO 和完整校验 |
| 选课系统 | 选课批次、可选课程、选课/退课、基础冲突判断和课程管理功能 | Access DAO、完整规则和并发容量验证 |
| 图书馆 | 线上检索与预约、我的图书馆、模拟自助借还、管理员维护、Access DAO 与事务/并发验证 | 委托、书评、书架和荐购等扩展功能 |
| 商店 | 商品查询/发布、闲鱼式商品展示、购物车、校园卡充值与支付、订单查询/取消、商家成交查询 | Access DAO 和库存并发验证 |
| 医院 | 科室/号源查询，三模式入口；医生申请、超级管理员审核、账号绑定和 Access 医生档案 | 预约主流程、排班管理、隐私与并发验证 |

## 3. 当前优先级

1. 学籍、选课、商店和医院逐步把内存 Repository 替换为 Access DAO。
2. 补齐每个受保护 Action 的登录、管理权或业务资格校验。
3. 完成正常、异常、越权和并发场景测试。
4. 在两台真实电脑和全新环境验证客户端 JAR、服务器 JAR、Radmin VPN、防火墙和单机数据库部署流程。

## 4. 主要风险

| 风险 | 当前处理 |
|---|---|
| Access/JDBC 集成晚，集中暴露问题 | 先做单模块最小读写验证，再逐模块迁移 |
| 模块只完成页面，没有服务器和数据链路 | PR 必须展示完整纵向链路和验证证据 |
| 只隐藏按钮，没有服务器授权 | 所有保护操作都在服务器 Action 再校验 |
| 库存、容量、号源并发出错 | 写操作放在服务器，并增加并发测试 |
| common 契约频繁变化造成冲突 | 公共协议小步修改，先评审再合并 |

## 5. 里程碑判断

- 公共骨架：已完成。
- 六模块首条纵向链路：进行中。
- Access 持久化、管理功能与跨模块联调：用户账号已接入，其余模块未完成。
- 功能冻结、部署验收和答辩材料：未开始。
