package edu.seu.vcampus.common.hospital;

/** Review state of a request to register a campus account as a hospital doctor. */
public enum DoctorApplicationStatus {
    PENDING,
    APPROVED,
    REJECTED
}
